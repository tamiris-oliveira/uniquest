UNIQUEST - Correção Disponível
===============================

Olá, <%= @user.name %>!

A correção de uma das suas respostas está disponível:

SIMULADO: <%= @simulation.title %>

DETALHES DA CORREÇÃO:
- Data da Tentativa: <%= @attempt.attempt_date.in_time_zone("America/Sao_Paulo").strftime("%d/%m/%Y às %H:%M") %> (horário de Brasília)
- Data da Correção: <%= @correction.correction_date.in_time_zone("America/Sao_Paulo").strftime("%d/%m/%Y às %H:%M") %> (horário de Brasília)
<% if @correction.grade.present? %>
- Nota Obtida: <%= @correction.grade.to_f.round(1) %>
<% end %>

QUESTÃO:
<%= truncate(@question.statement, length: 300) %>
<% if @question.statement.length > 300 %>
... (questão completa disponível na plataforma)
<% end %>

SUA RESPOSTA:
<%= @answer.student_answer %>

<% if @answer.correct.present? %>
STATUS: <%= @answer.correct? ? 'CORRETA' : 'INCORRETA' %>
<% end %>

<% if @correction.feedback.present? %>
FEEDBACK DO PROFESSOR:
<%= @correction.feedback %>
<% end %>

Veja os detalhes completos em: <%= @correction_url %>

Ou acesse a plataforma Uniquest: <%= @frontend_url %>

Continue estudando! Use este feedback para melhorar seu desempenho nas próximas tentativas.

---
Este é um email automático do sistema Uniquest.
Se você não deveria ter recebido este email, entre em contato conosco.